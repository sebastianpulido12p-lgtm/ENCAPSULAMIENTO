# Yojan Sebastian Galeano Pulido

# Aprendí a aplicar el encapsulamiento en PHP utilizando atributos privados (`private`) y métodos `get` y `set` para acceder y modificar la información de los objetos de manera segura y organizada.